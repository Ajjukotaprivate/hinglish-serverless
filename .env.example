# Supabase (required for Next.js app)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Railway API – must be the ffmpeg-api service URL (Root Directory: ffmpeg-api).
# If you have two services, use the API service’s domain, not the Next.js one.
NEXT_PUBLIC_RAILWAY_API_URL=https://your-ffmpeg-api-service.up.railway.app
